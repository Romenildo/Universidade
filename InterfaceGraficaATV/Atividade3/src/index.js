import React, {Component} from "react";
import App from "./atividade3/App";
import ReactDOM from 'react-dom';


ReactDOM.render(
    <React.StrictMode>
        <App/>
    </React.StrictMode>,
    document.getElementById("root")
);
